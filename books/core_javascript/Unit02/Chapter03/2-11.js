// so many line

console.log(sum(3, 4));

function sum (x, y) {
    return x + y;
}

// so many line

var a = sum(1, 2);
console.log(a);

// so many line

function sum (x, y) {
    return x + ' + ' + y + ' = ' + ( x + y );
}

var c = sum(1, 2);
console.log(c);
