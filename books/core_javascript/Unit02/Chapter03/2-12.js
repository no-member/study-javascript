// so many line

console.log(sum(3, 4));

var sum = function (x, y) {
    return x + y;
};

// so many line

var a = sum(1, 2);

// so many line

var sum = function (x, y) {
    return x + ' + ' + y + ' = ' + ( x + y );
};

var c = sum(1, 2);
console.log(c);

